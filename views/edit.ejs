<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <h1 class="text-3xl tracking-tighter mb-3">Update User</h1>
        <div>
            <form action="/update/<%= user._id %>" method="post">
                <input type="text" class="px-5 py-3 border-2 border-zinc-800 outline-none rounded-lg bg-transparent" placeholder="locality" name="locality" value="<%= user.locality %>">
                <input type="text" class="px-5 py-3 border-2 border-zinc-800 outline-none rounded-lg bg-transparent" placeholder="name" name="name" value="<%= user.name %>">
                <input type="text" class="px-5 py-3 border-2 border-zinc-800 outline-none rounded-lg bg-transparent" placeholder="email" name="email" value="<%= user.email %>">
                <input type="text" class="px-5 py-3 border-2 border-zinc-800 outline-none rounded-lg bg-transparent" placeholder="common area pics" name="common" value="<%= user.common %>">
                <input type="text" class="px-5 py-3 border-2 border-zinc-800 outline-none rounded-lg bg-transparent" placeholder="available room pics" name="rooms" value="<%= user.rooms %>">
                <input type="text" class="px-5 py-3 border-2 border-zinc-800 outline-none rounded-lg bg-transparent" placeholder="enter fees" name="fees" value="<%= user.fees %>">
                <input type="text" class="px-5 py-3 border-2 border-zinc-800 outline-none rounded-lg bg-transparent" placeholder="enter ur contact no." name="contact" value="<%= user.contact %>">
                <input type="text" class="px-5 py-3 border-2 border-zinc-800 outline-none rounded-lg bg-transparent" placeholder="other details" name="other" value="<%= user.other %>">

                <input type="submit" class="text-black px-5 py-2 bg-yellow-500 rounded-lg ml-3" value="Update user">
            </form>
        </div>
        <div class="pt-10">
            <a class="text-red-400" href="/delete/<%= user._id %>">Delete User</a>
        </div>
    </div>
</body>
</html>